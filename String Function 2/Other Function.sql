-- Insert (Used to insert word/char between strings)
select insert('Rabindra is Boy',12,0,' good'); 
-- will be inserted at 12th Position.

-- Trim Function used to remove spaces.
Select Trim(' Priyansh ') as Trimmed_Word;

-- Repeat (used for repeating Words.)
Select Repeat('HA ',5);
